diff --git a/test_api.py b/test_api.py
index f05b67b..1b0d19e 100644
--- a/test_api.py
+++ b/test_api.py
@@ -7,20 +7,15 @@ from hello import app, _sum
 def test_sum():
     assert _sum(1, 2) == '3'
 
-def test_successful_api_call():
+@pytest.mark.parametrize('url,status_code', [
+    ('/?a=1&b=2', 200),
+    ('/?a=wrong&b=parameters', 400),
+    ('/', 400),
+])
+def test_api(url, status_code):
     client = app.test_client()
-    response = client.get('/?a=1&b=2')
-    assert response.status_code == 200
-
-def test_mandatory_params():
-    client = app.test_client()
-    response = client.get('/?a=wrong&b=parameters')
-    assert response.status_code == 400
-
-def test_missing_params():
-    client = app.test_client()
-    response = client.get('/')
-    assert response.status_code == 400
+    response = client.get(url)
+    assert response.status_code == status_code
 
 def test_only_get_is_allowed():
     client = app.test_client()
